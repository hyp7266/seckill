spring:
  thymeleaf:
    #????
    cache: false
  #?????
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/seckill?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: hsp
    hikari:
      #????
      pool-name: DateHikariCP
      #???????
      minimum-idle: 5
      #?????????????600000?10???
      idle-timeout: 180000
      #????????10
      maximum-pool-size: 10
      #????????????
      auto-commit: true
      #?????????0?????????1800000?30???
      max-lifetime: 1800000
      #?????????30000?30??
      connection-timeout: 30000
      #?????????????
      connection-test-query: SELECT 1

  redis:
    #服务器地址
    host: 47.122.23.246
    #端口
    port: 6379
    #数据库
    database: 0
    #超时时间
    timeout: 10000ms
    #密码
    lettuce:
      pool:
        #最大连接数，默认8
        max-active: 8
        #最大连接阻塞等待时间，默认-1
        max-wait: 10000ms
        #最大空闲连接，默认8
        max-idle: 200
        #最小空闲连接，默认0
        min-idle: 5


mybatis-plus:
  #??Mapper.xml????
  mapper-locations: classpath*:/mapper/*Mapper.xml
  #??mybatis??????????
  type-aliases-package: com.xxxx.seckill1.pojo;

#mybatis sql??(???????????Mapper.xml????)
logging:
  level:
    com.xxxx.seckill1.mapper: debug
